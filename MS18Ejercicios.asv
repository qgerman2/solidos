%ejercicio 1 a
%fuerza externa uniaxial permite decir que esfuerzo = F / A0
F = 10000; %newton
A = pi * (50^2 - 3^2) / 4; %mm2
o = [1, 0, 0;
    0, 0, 0;
    0, 0, 0];
%rotar tensor esfuerzo 30 grados
o_30grados = rotar_plano(o, 30);
def = flexibilidad(210000, 0.27) * tensor_a_vector(o_30grados);
e = vector_a_tensor(def) * 10 ^ 4;

